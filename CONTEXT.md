# ats-status 术语表

本项目唯一的领域词汇表。实现细节一律不写这里。

## 投递

- **投递记录 (Application Record)**:一条「我向某公司投了某岗位」的事实,带当前状态。两个来源:**自动抓取**(招聘站点官网)与**手工登记**(六家之外的公司,人工录入,或由岗位库「已投」联动落条,见 ADR-0007)。同一家公司可以有多条投递记录(不同岗位、校招/社招)。
- **状态来源优先级**:人工修正 > 官网自动抓取。没有第三种自动来源(见 ADR-0003)。
- **人工修正 (Correction)**:人工对某条投递记录状态的覆盖,压得住自动抓取的结果;可还原。

## 邮件

- **邮件事件 (Mail Event)**:从邮箱拉取、被识别为招聘相关的一封邮件,含事件类型(OFFER/INTERVIEW/EXAM/REJECTED/…)。
- **邀约安排 (Schedule Tip)**:从笔试/面试邮件正文提取出的安排——轮次、时间、地点。日期+时间都识别到才产出,宁缺毋滥。

## 关联

- **挂载 (Attach)**:把邮件事件作为证据附着在投递记录旁展示。只读,不改变记录状态。
- **回写 (Writeback)**:用邮件事件更新投递记录的状态。本项目**不做自动回写**;人工辅助回写 = 由邮件预填人工修正,人确认后生效。
- **绑定 (Binding)**:把某条邮件事件与某条投递记录固定关联的人工记录。自动匹配只在**无歧义**(该公司恰好一条投递记录)时发生。
- **歧义 (Ambiguous)**:同一家公司存在多条投递记录,公司级匹配无法落到具体岗位。
- **未关联池 (Unlinked Pool)**:无法自动关联的邮件事件——歧义、无对应投递记录、或来源是平台系标签。
- **平台系标签 (Platform Label)**:一个邮件来源标签覆盖多家公司(如「Moka 系」含携程/知乎)。平台系标签**永不自动关联**。

## 岗位库(控制台)

- **岗位源 (Job Source)**:岗位内容的上游,可多个——**岗位包**(原厂策展,默认,所有用户开箱即用)与**文档源**(腾讯智能表格)。控制台侧只是只读缓存;岗位按来源分桶(`src`),分源整表替换。
- **岗位包 (Job Pack)**:原厂策展的岗位快照(jobs-pack.json),随仓库/安装包分发;不含文档地址与本地标记。用户点「同步」即拉最新(远程不可达回落随包内置版本)。
- **文档源 (Doc Source)**:用户自助添加的腾讯智能表格(需「有链接即可查看」,添加时试读预览确认表头可识别)。手动同步;移除时其岗位一并移除、本机标记保留。维护者的**主文档源**(发布岗位包的内容来源)受保护不可删。
- **发布 (Publish)**:维护者把主文档源变成新岗位包的流水线——同步→防呆检查→打包→提交推送;由定时器或手动触发。用户自助源的内容**不进岗位包**。
- **发布防呆 (Publish Guard)**:岗位数低于绝对下限、或较上一版骤降过半时拒绝发布——文档被误清空时绝不发坏包。
- **岗位库 (Job Pool,控制台)**:全部岗位源在本机的镜像(jobs.json),供浏览/搜索/筛选/截止提醒;手动同步,不自动轮询。
- **展示窗口 (Job Window)**:控制台界面只展示最近更新的 N 条;全量数据仍在本机库中,供发布导出。
- **手动收录 (Manual Source)**:内建岗位源(不可删)。「识别投递网址」与人工录入的岗位落点,只存本机,**永不进岗位包**。
- **识别投递网址 (Job Capture)**:在岗位详情页由「岗位收录助手」插件识别公司/岗位/城市,跳转控制台确认(或直接收录)后入「手动收录」。控制台不提供粘贴网址入口;识别引擎移植自台账「一键收录插件」,CLI `ats jobs capture <url>` 保留无头识别。识别失败宁可不猜。
- **岗位标记 (Job Mark)**:用户对某条岗位的本地标记——已投/不投/关注。覆盖层,按岗位 id 存,重同步/换源/移除源都保留,**永不写回岗位源**。例外:「已投」除标记外在投递记录中落一条带来源的条目,取消时按纯净度回收(见 ADR-0007);不投/关注仍是纯标记。
- **内推码 (Referral Code)**:岗位的内推码与联系人。维护者本机全量可见;**默认不进岗位包**(公开分发边界),`--full` 保留。
- 与台账(校招投递管理)的岗位库是两套独立数据:那套在浏览器本地、由「一键收录插件」同步共享文档;本套不推送给台账。
