# Changelog

## 未发布

- 插件 v1.1.2 修复公司识别:剥 title 通用后缀再取公司——「XX内部推荐官网」「XX校园招聘」「XX招聘官网」此前被过滤规则整体拒识别(Moka 内推页/官网招聘页公司名为空);平台名单补 Moka/北森/飞书招聘;服务端 jobcapture 同步
- 岗位源栏不再展示「手动收录」标签(用户口径):它是内建落点而非可管理的外部来源,插件收录的岗位照常出现在列表与统计;编辑弹窗内仍可见
- 岗位源标签重排(视觉):统一为 图标+名称+元信息+行内小动作 同一形态——文档源补条数、时间缩短为 09-12 12:45 格式、↻/✕ 改为默认隐形悬停点亮的轻量钮(移除钮悬停才变红)、台账导入源独立形态只读展示
- 岗位库内推列改内推链接(用户口径:不展示裸码):链接已含内推参数的原链接即内推链接;干净链接有码按域名参数表拼接(Moka→recommendCode/163→referralCode/小红书→referer_code/fandow→pushCode,表外默认 recommendCode,hash 路由与 query 均支持)显示「内推 ↗」;链接为自由文本或缺失的回退显示码+复制;完整链接/码/联系人悬停可见。纯前端展示,岗位包剥离策略不变
- 投递总览「改」并入「编辑」:两类行操作统一为 推进/编辑/归档——站点行编辑弹窗=状态人工修正子功能(仅状态可改,显示官网当前状态,已修正的可一键还原官网;公司/岗位/时间为镜像数据不可改),apps 行编辑弹窗不变;旧内嵌下拉与 mailCorrect 预填流程迁移到新弹窗
- 状态词汇统一为七档(用户口径):已投递/评估中/笔试/面试/HR面/Offer/已结束——筛选下拉、「改」「编辑」下拉、徽章、分布图同一套;筛选是评估中(含已查看/筛选中),面试含复试,HR面独立成档;旧细分码在预选时自动折算(已查看→评估中/复试→面试/不合适·人才池→已结束);站点行推进写新标签(评估中/面试)
- 投递总览操作栏降噪:大蓝按钮改为小号 soft「推进」+文字「改/归档/编辑」(新增 act-sm 轻量变体),固定一行不折行;「改」编辑态的下拉与按钮同尺寸
- 状态五档化(用户真实反馈:不合适/人才池/已关闭「都属于一个状态」,只想知道 已投递/笔试/面试中/Offer/已结束):徽章与分布图按五档桶显示(原文悬停可见);状态筛选改五档桶;「已结束」默认视图隐藏、等价归档(零数据改动,状态筛「已结束」或归档筛「全部」可看回;Offer 保留在默认视图);推进阶梯同步按桶走(已投递→笔试→面试中→Offer,每点一次跨一个可见状态)
- 投递总览操作栏对齐投递管理台账:「推进」一键沿 已投递→已查看→筛选中→笔试→面试中→复试→HR面→Offer 阶梯走一步(站点行写人工修正覆盖层=官网更新不覆盖,apps 行直接改本机记录;Offer/终态后无推进),「编辑」给手工/岗位库行补真弹窗(公司/岗位/状态/投递时间/链接,改名撞车保护),替换原「编辑 apps.json」死提示;状态归一化补 一面/二面/复试/HR面/终面 → 面试中(迁移自台账的阶段此前显示为未知灰色)
- 投递总览模糊搜索:筛选行新增搜索框,空格分隔多词 AND,每词对 公司/岗位/部门/城市/状态/来源 归一化后(去空白与中英文标点、小写)子串命中——「tabbitai」能命中「Tabbit AI浏览器」,「美团 已投递」能同时按公司与状态收窄;与公司/状态/归档筛选叠加生效,计数与空态照常
- 岗位库可编辑:手动收录与台账导入的岗位(本机自有源)行内「✎」直接改公司/岗位/城市/截止/链接/内推/备注,截止支持标准日期自动归一或保留原文;来自腾讯文档/岗位包的同步源岗位明确拒绝编辑(改了会被下次同步覆盖),标记与岗位 id 不受编辑影响
- 岗位库表格改固定布局:各列定宽+「岗位」独占余量,「更新」列在主流窗口宽度(≥1100px)不再被水平裁掉(此前表格自动布局被长岗位名撑到 1203px,最右列被裁,macOS 隐形滚动条让人以为列不存在);更新单元格悬停显示完整时间戳
- 插件悬浮球(v1.1.0,可选开启):岗位页右下角自动浮现 ⌕ 小球,点开面板识别+收录,免找工具栏;仅在「长得像岗位页」时出现(非岗位页/控制台页不浮现),Shadow DOM 隔离样式,可拖动记忆位置;经 popup 开关二次授权(optional_host_permissions)后动态注册,默认不注入、关闭即撤回权限;识别引擎抽为 common.js 三处共用
- 收录即投递(ADR-0007 追记):「岗位收录助手」确认收录默认同时记为已投——岗位库条目+已投标记+投递总览记录一次完成(插件升 v1.0.1,弹窗新增「同时记为已投递」勾选,默认开,取消即纯收藏);重复收录同岗位不重复建记录;台账插件的投递日期/阶段推断暂不移植
- 投递总览显示补全:「已投」联动产生的记录此前「数据时间」列显示 —(投递时间已存但未渲染)、地点列为空——现在数据时间显示投递日期(apps 行通用,含手工登记),新建联动记录同时带上岗位城市

## 1.2.0 (2026-09-12)

- 台账迁移:支持把「校招投递管理」台账数据迁进控制台——文件式(导入台账「导出数据」json)与一键式(台账「数据安全 → 迁移到校招投递控制台」,同源托管时零配置)双通道;投递记录幂等合并(同公司同岗位=更新),台账岗位库进专用「台账岗位库」源,「不投」标记带过来;台账侧 envelope 同步携带岗位库
- 岗位库「已投」联动投递总览(ADR-0007):点「已投」= 标记 + 自动在投递记录落一条(状态「已投递」、当天日期、岗位链接,来源显示「岗位库」);同公司同岗位已有记录则只标记;取消标记时,未进展的记录一并回收,已改状态/绑过邮件的保留并提示;手工登记与迁移来的记录永不被删。反向仍走「可能已投」提示式,不自动点亮
- **岗位收录助手**(新浏览器插件,plugin/,MV3):在岗位详情页点图标即识别公司/岗位/城市(可改),「在控制台确认收录」跳控制台核对或「直接收录」落库——识别在页面内完成,结果只发本机 127.0.0.1:7788,零外部依赖;扩展 ID 固定(manifest key),图标纯 node 生成,CI release 顺带打包
- 识别投递网址(识别引擎移植自台账「一键收录插件」):入口统一为插件;控制台不提供粘贴网址入口,CLI `ats jobs capture <url>` 保留;识别引擎含 JSON-LD JobPosting/og meta/选择器三级提取+城市白名单与岗位词推断,SPA 等渲染按岗位信号轮询;同内容重复收录=更新;台账原插件保持不动
- 岗位库多源:用户可自助添加任意腾讯智能表格岗位源(试读预览确认后入库,分源同步/移除,标记保留);新式项目文档(clientVars.padId)与公告板型表格(公司嵌在公告标题)两类形态支持;「登录腾讯文档」扫码一次后私密文档源亦可同步;岗位包只含维护者主文档源(ADR-0006 追记)

- 岗位库「已投」联动投递总览(ADR-0007):点「已投」= 标记 + 自动在投递记录落一条(状态「已投递」、当天日期、岗位链接,来源显示「岗位库」);同公司同岗位已有记录则只标记;取消标记时,未进展的记录一并回收,已改状态/绑过邮件的保留并提示;手工登记与迁移来的记录永不被删。反向仍走「可能已投」提示式,不自动点亮
- **岗位收录助手**(新浏览器插件,plugin/,MV3):在岗位详情页点图标即识别公司/岗位/城市(可改)一键收录进控制台「手动收录」源——识别在页面内完成,结果只 POST 本机 127.0.0.1:7788,零外部依赖;扩展 ID 固定(manifest key),图标纯 node 生成,CI release 顺带打包
- 识别投递网址(识别引擎移植自台账「一键收录插件」):入口统一为「岗位收录助手」插件——页内识别后跳控制台确认或直接收录;控制台不再提供粘贴网址入口,CLI `ats jobs capture <url>` 保留;识别引擎含 JSON-LD JobPosting/og meta/选择器三级提取+城市白名单与岗位词推断,SPA 等渲染按岗位信号轮询;同内容重复收录=更新;台账原插件保持不动
- 岗位库多源:用户可自助添加任意腾讯智能表格岗位源(试读预览确认后入库,分源同步/移除,标记保留);新式项目文档(clientVars.padId)与公告板型表格(公司嵌在公告标题)两类形态支持;「登录腾讯文档」扫码一次后私密文档源亦可同步;岗位包只含维护者主文档源(ADR-0006 追记)

## 1.1.1 (2026-09-11)

- 上手检查单:新用户打开控制台即见四项清单(接入第一家/查一次状态/配邮箱可选/同步岗位库),第一家按邮箱信号智能推荐(快手兜底),必做完成即毕业;激活漏斗 P0(ROADMAP)
- 「可能已投」:官网已有这家公司在途投递时,岗位行提示一键确认(归一化互相包含,≥4 字防误报;只提示不落标)
- 站点失效告警:连续 2 次查询失败 → 站点卡片亮「⚠ 接口疑似失效」+ 修复指引
- 岗位库显示岗位包生成时间;README 明确更新频率口径(尽力而为,通常每工作日两次)

## 1.1.0 (2026-09-11)

- 岗位库(控制台新面板):岗位源固定绑定一份腾讯智能表格(地址存 jobs.json,不提供界面配置;文档需「有链接即可查看」),复刻台账插件的 dop-api 读取链路,匿名无头手动同步;列映射精确匹配固定列(公司/岗位/城市/截止时间/投递链接/备注)并有别名兜底(含内推码/联系人/批次、17 类选项字段),截止时间归一并做「七天临近/已截止」提醒
- 岗位包分发机制:对外只发布 `jobs-pack.json` 数据快照,普通用户零配置开箱即用;默认走公开仓库 raw 直链远程更新(点同步即最新,无需重装;离线/不可达回落安装包内置包),`jobs.json` 的 packUrl 可自定义远程地址;维护者的智能表格地址永不分发(ADR-0006)。导出命令 `ats jobs pack`,默认剥离内推码/联系人字段并清洗链接里的内推参数(recommendCode/referralCode,含 SPA hash 内查询串),岗位 ID 按清洗后内容重算,换内推码不漂移;`--full` 保留全部
- 岗位包自动发布流水线(维护者):`ats jobs publish` = 同步文档→防呆检查→打包→git 提交推送(只动 jobs-pack.json,推送失败已提交本地并通知);控制台可一键安装/卸载定时发布(macOS LaunchAgent / Windows schtasks,每日 12:00 / 21:30 curl 本地 /api/jobs/publish,与查询走同一 runJob 守卫);岗位数低于下限或较上一版骤降过半自动拦截并发系统通知
- 岗位大表全量翻页:腾讯文档 get/sheet 续页使用 {t:3028} 封套且载荷可能为已解析对象,漏识别导致只读到第一页;现按响应 maxrow 全量翻页,实测 2456 行全量拉通
- 岗位库列表:每页 20 条分页;按文档「更新日期」列倒序(最新在最前);决策序展示——截止信号分级(临近红标/正常日期/招满即止弱化)、内推码独立成列点击复制、批次徽章、岗位城市单行截断悬浮全文;只展示最近更新的 250 条,全量留本机供发布
- 「已投/不投/关注」本地标记覆盖层,重同步整表替换岗位后标记保留;CLI 同能力:`ats jobs / jobs sync / jobs pack / jobs publish`
- 投递总览:分页 + 公司/状态/归档三筛选器,非结束状态优先展示;归档记录成区排在末尾;删冗余「原始状态」列;日期缩略
- 界面统一:侧栏与版块标题一一对应(站点接入/邮件通知/设置与帮手),删除自指向的「高级与诊断」遗留入口;设置区补面板标题头

## 1.0.0 (2026-09-10)

首个公开发布。

- 六站聚合:字节 / 阿里 / 美团 / 携程 / 小红书 / 快手官网投递状态一键查询,API 直连失败自动降级页面模式;支持自定义站点与手工登记(apps.json)
- 本地控制台(`npm run ui`):常驻登录、抓包向导、查询、总览、人工修正/归档,全部点按钮
- 邮件监控:IMAP 多邮箱,招聘邮件分类(Offer/面试/笔试/感谢信…),笔试面试正文提取轮次/时间/地点,挂载到对应投递记录,邀约系统通知
- 邮件↔投递关联:公司无歧义自动匹配,歧义人工绑定(ADR-0003 只挂载不自动回写)
- 记录身份含部门;人工修正/归档双键兼容旧数据
- 保活:12h ±40% 抖动、夜间静默、掉线降频不加密
- 定时任务:macOS LaunchAgent / Windows schtasks,每日三时点查询 + 通知 + 邮件
- 状态包同步(tracker-sync)给「校招投递管理」台账;飞书通知可选
- 单元测试(node --test)、Windows 冒烟 CI
